{{#>boilerplate}}

<main>

    <h1>Hello!</h1>

    <p>
        <a href="{{URL}}"> Home </a>
    </p>

    <h2>List of Numbers</h2>

    <p>
        {{#each list}}
        {{this}}{{#unless @last}},{{/unless}}
        {{/each}}
    </p>

</main>

{{/boilerplate}}